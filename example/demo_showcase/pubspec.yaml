name: demo_showcase
description: MCP UI DSL widget showcase — reference implementation for all widget types (AppPlayer example copy of apps/demo_ui)
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  mcp_server: ^2.0.0
